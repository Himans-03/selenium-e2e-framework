[pytest]
# Where to look for tests
testpaths = tests

# Verbose output so you can see each test name pass/fail
addopts =
    --verbose
    --html=reports/report.html
    --self-contained-html

# Show extra summary info for failures and errors
log_cli = true
log_cli_level = INFO
